# Simple API Client — install from repo root:
#   pip install -r requirements.txt
#
# Runtime: Python 3.11+ standard library only (no third-party packages).
# Editable install registers the `apiclient` CLI.

-e .

# Development / testing (matches pyproject.toml [project.optional-dependencies].dev)
pytest>=8
pytest-cov>=5
ruff>=0.6
